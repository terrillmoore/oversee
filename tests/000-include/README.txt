The C files in this directory were generated by the following line:

for i in ../../i/*.h ; do echo $i ; printf "#include \x22%s\x22\n" $i > $(basename $i).c ; done

The "\x22" dodge puts in the proper quotes, no matter the shell.
